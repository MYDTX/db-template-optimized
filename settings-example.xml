# Maven Settings 配置示例
# 将此文件复制到 ~/.m2/settings.xml 并修改相应的用户名和密码

<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">

    <!-- Maven Central 发布配置 -->
    <servers>
        <!-- Sonatype OSSRH (用于发布到 Maven Central) -->
        <server>
            <id>ossrh</id>
            <username>你的Sonatype用户名</username>
            <password>你的Sonatype密码</password>
        </server>

        <!-- 用于 central-publishing-maven-plugin -->
        <server>
            <id>central</id>
            <username>你的Sonatype用户名</username>
            <password>你的Sonatype密码</password>
        </server>
    </servers>

    <!-- GPG 签名配置（可选，也可以在命令行中指定） -->
    <profiles>
        <profile>
            <id>gpg-signing</id>
            <properties>
                <gpg.executable>gpg</gpg.executable>
                <gpg.passphrase>你的GPG密钥密码</gpg.passphrase>
            </properties>
        </profile>
    </profiles>

    <activeProfiles>
        <activeProfile>gpg-signing</activeProfile>
    </activeProfiles>
</settings>
